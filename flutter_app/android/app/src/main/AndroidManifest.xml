<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>
    <uses-permission android:name="android.permission.READ_SMS"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- ═══ 局域网发现（★必需，勿删）═══
         引擎靠 UDP 19527 广播自报(THP/1 HELLO)。Android 的 WiFi 固件在省电模式下
         会**静默丢弃入站的广播/组播帧**（熄屏后尤其明显）→ 表现为"有时能发现引擎、
         锁屏一会儿就没了"。要收得住，必须持有 WifiManager.MulticastLock，
         而创建该锁需要 CHANGE_WIFI_MULTICAST_STATE；读网段/网关需要 ACCESS_WIFI_STATE。
         三者缺一，发现层就退化成"只能靠主动扫描"，广播这条快路等于没接。 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <!-- 相册: Android13+ 细分媒体权限 / 13以下旧权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29"/>
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION"/>
    <!-- 通知: 公告/下载完成提醒 + 音乐通知栏控制 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- ═══ 局域网明文 HTTP 放行（★必需，勿删）═══
         本 App 的核心链路是「前端直连局域网引擎」：引擎是用户自己设备上跑的 THP 服务
         (阅读引擎 :1234 / 资源库 :9527)，地址为运行时发现的局域网 IP，无法提前枚举域名；
         引擎侧只提供 http。targetSdk=36 时 Android 默认禁止明文，
         会把所有请求拦在系统层（报 CLEARTEXT communication not permitted，
         Dart 侧表现为 http.ClientException），表现为「连上了却一直显示未连接引擎 + 搜索无结果」。
         细化策略见 res/xml/network_security_config.xml。 -->
    <application android:label="第三方聚合" android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <!-- ═══ 画中画（★必需，勿删）═══
             supportsPictureInPicture 不开，则 enterPictureInPictureMode() 直接抛
             IllegalStateException（Dart 侧表现为「点了画中画没反应」）；
             resizeableActivity 必须为 true——它一旦为 false，系统会拒绝把 Activity
             缩成小窗，且 7.0 起 Android TV 之外的设备也靠它判定可否分屏/浮窗。
             configChanges 里的 screenSize|smallestScreenSize|screenLayout 是画中画切换时
             必然发生的配置变化，不声明就会走 Activity 重建 → 播放器被销毁、画面黑掉。 -->
        <activity android:name=".MainActivity" android:exported="true"
            android:launchMode="singleTop" android:theme="@style/LaunchTheme"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true" android:windowSoftInputMode="adjustResize">
            <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- 打开方式: 网页链接 -> 内置浏览器 -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="http"/>
                <data android:scheme="https"/>
            </intent-filter>
            <!-- 打开方式: TXT/文本 -> 小说阅读器 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="file"/>
                <data android:scheme="content"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="file"/>
                <data android:scheme="content"/>
                <data android:host="*"/>
                <data android:pathPattern=".*\\.txt"/>
                <data android:pathPattern=".*\\.TXT"/>
                <data android:pathPattern=".*\\.epub"/>
                <data android:pathPattern=".*\\.EPUB"/>
            </intent-filter>
            <!-- 分享文本 -> 阅读器导入 -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <!-- 打开方式: 音频 -> 音乐播放器 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="file"/>
                <data android:scheme="content"/>
                <data android:mimeType="audio/*"/>
            </intent-filter>
            <!-- 打开方式: 视频 -> 视频播放器 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="file"/>
                <data android:scheme="content"/>
                <data android:mimeType="video/*"/>
            </intent-filter>
        </activity>
        <meta-data android:name="flutterEmbedding" android:value="2"/>
        <!-- APK 安装/open_filex 需要的 FileProvider -->
        <provider android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileProvider"
            android:exported="false" android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths"/>
        </provider>
        <!-- 音乐后台播放 + 通知栏控制(just_audio_background/audio_service) -->
        <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback" android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>
        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>
            <service android:name=".OverlayService" android:exported="false" />
    </application>
</manifest>
