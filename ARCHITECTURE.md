# ThirdHub v4.0.0 架构总纲 —— 局域网设备编排架构

> 版本: **4.0.0-draft** | 日期: 2026-09-14 | 状态: 设计冻结，待实现
> 本文件取代此前一切架构讨论，以本文件为准。

## 0. 设计哲学（三条铁律）

1. **前后端严格分离**：前端只有"显示"一个职责，任何引擎/规则/解析都不进前端。
2. **局域网编排，而非编译集成**：开源应用（Legado/Komga/Audiobookshelf）以
   **官方原版**运行在用户局域网内，家庭后端通过其官方 API 连接编排。
   不做壳中壳、不做包名重命名、不做编译集成——零侵入、零合并地狱。
3. **能力归一（IR）**：无论来源是网络插件还是内置引擎，输出必须转译为
   统一 IR（novel-ir/video-ir/music-ir/comic-ir），前端永远不感知来源差异。

## 1. 总体架构

```
┌──────────── 前端矩阵（同一份网页代码，套壳全平台）────────────┐
│  PWA(浏览器)  iOS/Android(Capacitor)  Win/Mac/Linux(Electron) │
└──────────────────────────┬───────────────────────────────────┘
                           │ THP 协议 (REST + WS + SSE, 可选加密信封)
┌──────────────────────────▼───────────────────────────────────┐
│                 家庭后端 :9527 (唯一核心)                     │
│  ┌──────────────┬──────────────┬──────────────┐               │
│  │ THP 网关     │ 穿透/局域网  │ CF账户验证   │               │
│  │ 能力路由     │ IPv6+DDNS    │ (低频)       │               │
│  │ PluginManager│ mDNS发现     │ 节点心跳     │               │
│  └──────────────┴──────────────┴──────────────┘               │
└──────────┬──────────────────────────────┬────────────────────┘
           │ 局域网编排协议               │ 进程内插件
┌──────────▼───────────────┐  ┌───────────▼────────────┐
│  网络设备插件(官方原版App)│  │  内置引擎插件           │
│  · Legado (:1122)        │  │  · drpy引擎(影视)       │
│  · Komga (:8080)         │  │  · venera引擎(漫画图源) │
│  · Audiobookshelf        │  │  · musicfree沙箱(音源)  │
│    (:13378)              │  │  (无API的开源项目跑规则)│
│  · 自定义设备(协议开放)  │  │                        │
└──────────────────────────┘  └────────────────────────┘
```

## 2. 分层职责

| 层 | 职责 | 非职责 |
|---|---|---|
| 前端 | 渲染 IR、用户交互、模式切换(直连↔后端) | 解析、引擎、规则 |
| 家庭后端 | 路由、编排、账户、穿透、IR 归一 | 内容存储(除缓存) |
| 网络设备插件 | 官方 App 提供能力(书源/漫画/有声书) | 不知道 ThirdHub 存在 |
| 内置引擎 | 跑无 API 开源项目的规则(JS沙箱) | UI |

关键原则：**网络设备插件不知道自己被编排**（它们只是开着官方 Web 服务）；
是后端主动去发现、协商、适配。这让插件永远跟随官方更新，零维护。

## 3. 网络设备编排协议（THP-Device）

### 3.1 发现层
- **mDNS 广播**：设备服务类型 `_thirdhub-dev._tcp.local.`，TXT 记录带
  `{type: legado|komga|audiobookshelf|custom, port, version, caps}`。
- **手动添加**：IP+端口+访问密钥，用于 mDNS 不可用的网络。
- **发现去重**：同一设备多网卡/多实例按 (uuid) 去重。

### 3.2 握手与能力协商
```
后端 → 设备: GET /capabilities (携带 backend nonce)
设备 → 后端: {device_id, type, version, caps:{novel:{search,detail,catalog,
             content}, comic:{...}, auth:"token"|"none"}}
后端: 校验 caps → 注册进设备表 → 生成设备路由记录
```
能力字典与 THP IR 一一对应，设备只声明自己会什么，后端负责映射。

### 3.3 会话与心跳
- 每设备独立 token（握手时下发或用户配置）。
- 心跳 30s，连续 3 次失败标记离线 → 触发降级路由。
- 设备离线不是错误，是路由切换信号。

### 3.4 适配器接口（后端内部）
```
interface DeviceAdapter {
  match(device): boolean          // 按 type/caps 认领
  capabilities(device): Caps      // 声明可桥接能力
  call(device, capability, args): IR  // 调用并归一为 THP IR
  health(device): Status
}
```
新设备类型 = 写一个适配器文件，核心零改动。

## 4. 能力路由（核心机制）

```
前端请求 /v1/novel/search?q=xx
  → 路由表查询: 哪些来源声明了 novel.search?
     ├─ 网络设备: Legado@192.168.1.5 (在线, 延迟23ms, 权重100)
     ├─ 内置引擎: legado-adapter (本地, 权重80)
  → 并行调用（每来源 3s 超时）→ 结果合并去重 → IR 归一 → 返回
来源不可用时自动摘除，恢复后自动回归。前端只见一个合并结果。
```

## 5. 前端矩阵（一套代码，五端出包）

- **代码**：Vue3 + Vite，PWA 为基线构建产物。
- **Win/Mac/Linux**：Electron 壳（托盘/自启），复用 PWA 构建产物。
- **Android/iOS**：Capacitor 壳；远期若性能不足再评估 Flutter 重写（当前不做）。
- **老 Kotlin 壳**：冻结退役，不再更新（v3.0.15 为最终版）。
- **低端机/手表**：PWA 走"低端机模式"（关动画/减特效）+ Chrome74 语法门禁。

## 6. 降级与容错矩阵

| 故障 | 行为 |
|---|---|
| Legado 离线 | 自动切内置 legado-adapter（若有书源），界面提示"已切换内置引擎" |
| 内置引擎失败 | 返回 action_required 或 source_error，附来源标识 |
| 家庭后端离线 | 前端直连模式兜底（浏览器沙箱跑 JS 源） |
| CF 账户服务离线 | 本地功能全可用，仅同步/多节点停用（12 次心跳宽限） |

## 7. 账户与数据主权

- CF Worker：仅低频（注册登录/心跳/版本检查，<5000 请求/天）。
- 用户内容数据：全部在用户局域网（Legado 自己的数据库/Komga 自己的库），
  后端只存设备路由表与缓存，云上不落地内容。
- 多节点：家庭后端之间走 Tailscale/IPv6 直连同步（设备表/设置/进度）。

## 8. 与旧方案的关系

| 旧方案 | 处置 |
|---|---|
| 壳中壳（编译集成 Legado/TVBox） | **作废**。legado-thirdhub/tvbox-thirdhub 两仓降级为"规则数据镜像" |
| Flutter 全面重写 | 降级为"远期选项"，当前前端=PWA+套壳 |
| TVBox App 集成 | 取消。TVBox 无 API，影视=内置 drpy 引擎+源数据包 |
| 后端收敛（两线归位） | **继续有效**：9527 个人版 / family-server 家庭多人版 |

## 9. 实施路线图

| 阶段 | 内容 | 验收 |
|---|---|---|
| P1 | 设备协议骨架 + mDNS 发现 + Legado 适配器 | 局域网 Legado 接入，前端搜书看书 |
| P2 | Komga/Audiobookshelf 适配器 + 能力路由并行合并 | 多设备并联搜索 |
| P3 | 内置引擎与网络设备统一路由（drpy/venera/musicfree） | 影视/漫画/音源全通 |
| P4 | 前端五端套壳出包 + 低端机模式 | 全平台安装包 |
| P5 | 多节点互联 + BYOC(CF OAuth) | 跨家庭共享设备 |

## 10. 并行开发分工（可拆给不同 AI）

- 组A：THP-Device 协议 + mDNS + 设备表（后端）
- 组B：Legado 适配器（验证 1122 API 端点清单 → IR 映射）
- 组C：能力路由/并行合并/降级矩阵（后端）
- 组D：前端矩阵套壳（Electron/Capacitor 构建链）
- 组E：内置引擎收编（drpy/venera/musicfree 入 PluginManager）

各组只依赖本文档第 3、4 节的协议定义，可完全并行。
